# ===========================================================================
# EduPro - development / research-tooling dependencies
# ===========================================================================
# Install on top of requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
#
# These are NOT needed to run the Streamlit application and are deliberately
# excluded from requirements.txt to keep the deployment footprint minimal.
# ===========================================================================

# --- testing ---------------------------------------------------------------
pytest==9.1.1

# --- notebooks (Phase 1-3 exploration; see CLAUDE.md s19) -------------------
jupyterlab==4.6.3
ipykernel==7.3.0

# --- reading the official documentation PDF --------------------------------
# The official Unified Mentor PDF is image-based (no embedded text layer),
# so it must be rendered to images to be read. Used by scripts/ tooling only.
pypdf==6.19.0
pymupdf==1.28.2

# --- rendering the research paper to a submission-ready HTML document -------
# Reached transitively via jupyterlab, but declared explicitly so that
# scripts/build_paper.py is reproducible rather than depending on an
# incidental transitive install.
mistune==3.3.4
