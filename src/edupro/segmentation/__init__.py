"""Learner segmentation.

Responsibilities: scaling/encoding, K-Means (primary), hierarchical clustering
(validation), cluster-count selection via elbow and silhouette analysis,
stability assessment, and interpretable cluster profiling.
"""
